
    }
    $scope.test = function () {
        if ($scope.passw1 !== $scope.passw2)
            $scope.error = true;
        else
            $scope.error = false;
        $scope.incomplete = false;
        if ($scope.edit && (!$scope.fName.length || !$scope.lName.length || !$scope.pass1.length ||!$scope.passw2.length))
            $scope.incomplete = true;

    };
});

mani